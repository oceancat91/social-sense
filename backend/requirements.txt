flask>=3.0
flask-cors>=4.0
flask-sqlalchemy>=3.1
flask-jwt-extended>=4.6
pymysql>=1.1
redis>=5.0
python-dotenv>=1.0
requests>=2.31
pandas>=2.1
jieba>=0.42
transformers>=4.36
gunicorn>=21.2
# 预训练情感模型推理后端（可选，不安装则自动降级为词典分析）
# CPU 版本安装：pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0
